#include "main.h"

/**
  * _islower - check the code
  * @v: for vesta
  *
  * Return: 1 for lower or 0 for upper
  */
int _islower(int v)
{
	if (v >= 97 && v <= 122)
	{
		return (1);
	}

	return (0);
}
